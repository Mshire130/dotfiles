eval "$(pyenv init -)"

if [ -f $(brew --prefix)/etc/bash_completion ]; then
    . $(brew --prefix)/etc/bash_completion
fi

export PYPI_PASSWORD="APAJ6BoccduVSFyesEFywFHRTjf" 
export PYPI_USER="mshire"